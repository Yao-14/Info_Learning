'''
字符串的替换方法
# string.replace(str1,str2,num)  把字符串中的str1替换成str2，如果num指定，则替换不超过num次
# string.expandtabs(tabsize=8)   把字符串中的tab符号转为空格，tab符号默认的空格数是8
# string.translate(str,del="")   根据str给出的表（包含256个字符）转换字符串的字符，要过滤掉的字符放到del参数中
# string.maketrans(intab,outtab) 用于创建字符映射的转换表，对于接受两个参数的最简单的调用方式，第一个参数是字符串，表示需要转换的字符，第二个参数也是字符串表示转换的目标

'''

