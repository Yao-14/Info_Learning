'''

# 判断目录或文件
os.path.isfile(path)    # 判断path是否为文件，是返回Ture，不是返回False
os.path.isdir(path)     # 判断path是否为文件夹，是返回Ture，不是返回False
os.path.islink(path)    # 判断path是否为链接，是返回Ture，不是返回False
os.path.obs(path)       # 判断path是否为绝对路径，是返回Ture，不是返回False
'''